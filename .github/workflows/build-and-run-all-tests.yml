# Build and then run all tests, on all supported archs.
name: Build and run all tests

on:
  workflow_call:
    inputs:
      build-type:
        required: true
        type: string
      timeout:
        required: true
        type: number
      log-params:
        required: false
        type: string
        default: ""
  workflow_dispatch:
    inputs:
      build-type:
        required: true
        default: Release
        type: choice
        options:
          - Release
          - RelWithDebInfo
          - Debug
          - ASan
          - TSan
      log-level:
        required: false
        default: info
        type: choice
        options:
          - info
          - debug
          - trace
      timeout:
        required: true
        description: 'The timeout for the test run in minutes'
        type: number
        default: 30
      # Individual test suite timeouts (in seconds)
      api-test-timeout:
        required: false
        type: number
        default: 70  # 1 minute + 10% tolerance
      baremetal-test-timeout:
        required: false
        type: number
        default: 300  # 5 minutes
      umd-unit-test-timeout:
        required: false
        type: number
        default: 180  # 3 minutes
      pci-test-timeout:
        required: false
        type: number
        default: 180  # 3 minutes
      misc-test-timeout:
        required: false
        type: number
        default: 180  # 3 minutes
      unified-test-timeout:
        required: false
        type: number
        default: 300  # 5 minutes


jobs:
  build-tests:
    name: ""
    secrets: inherit
    strategy:
      fail-fast: false
      matrix:
        ubuntu-docker-version: [
          'ubuntu-22.04',
        ]
    uses: ./.github/workflows/build-tests.yml
    with:
      ubuntu-docker-version: ${{ matrix.ubuntu-docker-version}}
      timeout: 15
      build-type: ${{ inputs.build-type }}
      log-params: |
        ${{ inputs.log-params }}
        echo build-and-run-all-tests - build-type: ${{ inputs.build-type }}
        echo build-and-run-all-tests - timeout: ${{ inputs.timeout }}
        echo build-and-run-all-tests - log-level: ${{ inputs.log-level }}


  test-all:
    name: ""
    secrets: inherit
    needs: build-tests
    strategy:
      fail-fast: false
      matrix:
        test-group: [
          {arch: wormhole_b0, card: tt-ubuntu-2204-n150-stable},
          {arch: wormhole_b0, card: tt-ubuntu-2204-n150-viommu-stable},
          {arch: wormhole_b0, card: tt-ubuntu-2204-n300-stable},
          {arch: wormhole_b0, card: tt-ubuntu-2204-n300-viommu-stable},
          {arch: wormhole_b0, card: tt-ubuntu-2204-n300-llmbox-stable},
          {arch: wormhole_b0, card: tt-ubuntu-2204-n300-llmbox-viommu-stable},
          # TODO: To be added when available.
          # {arch: blackhole, card: tt-ubuntu-2204-p100a-stable},
          {arch: blackhole, card: tt-ubuntu-2204-p100a-viommu-stable},
          {arch: blackhole, card: tt-ubuntu-2204-p150b-stable},
          {arch: blackhole, card: tt-ubuntu-2204-p150b-viommu-stable},
          {arch: wormhole_b0, card: 6u},
          # bh-gh-07 is a P300 runner.
          {arch: blackhole, card: bh-gh-07},
          {arch: baremetal, card: ubuntu-22.04},
        ]
        ubuntu-docker-version: [
          'ubuntu-22.04',
        ]
    uses: ./.github/workflows/run-tests.yml
    with:
      arch: ${{ matrix.test-group.arch}}
      ubuntu-docker-version: ${{ matrix.ubuntu-docker-version}}
      card: ${{ matrix.test-group.card}}
      timeout: ${{ fromJSON(inputs.timeout) }}
      log-level: ${{ inputs.log-level || 'info' }}
      build-type: ${{ inputs.build-type }}
      # Pass through individual test suite timeouts
      api-test-timeout: ${{ inputs.api-test-timeout }}
      baremetal-test-timeout: ${{ inputs.baremetal-test-timeout }}
      umd-unit-test-timeout: ${{ inputs.umd-unit-test-timeout }}
      pci-test-timeout: ${{ inputs.pci-test-timeout }}
      misc-test-timeout: ${{ inputs.misc-test-timeout }}
      unified-test-timeout: ${{ inputs.unified-test-timeout }}
      log-params: |
        ${{ inputs.log-params }}
        echo build-and-run-all-tests - build-type: ${{ inputs.build-type }}
        echo build-and-run-all-tests - timeout: ${{ inputs.timeout }}
        echo build-and-run-all-tests - log-level: ${{ inputs.log-level }}
