# UMD Base API Layer Mapping

## Chip / LocalChip / RemoteChip → TTDevice

<table>
<tr><th><b>API</b></th><th><b>Parameters / Return</b></th><th><b>UMD Workload</b></th><th><b>UMD Base Layer</b></th><th><b>Description</b></th></tr>
<tr><td>constructor()</td><td><b>Parameters:</b> <br> - chip_info: <code>ChipInfo</code> — device identification metadata <br> - soc_descriptor: <code>SocDescriptor</code> — SOC configuration <br> <b>Returns:</b> <br> - N/A</td><td>1. Set power state to high (<code>TTDevice::set_power_state(true)</code>) <br> 2. Wait for chip to be ready: <br> &nbsp;&nbsp;• eth core training (<code>TTDevice::wait_eth_core_training()</code>) <br> &nbsp;&nbsp;• dram core training (<code>TTDevice::wait_dram_channel_training()</code>) <br> 3. Init default chip mutexes (<code>initialize_default_chip_mutexes()</code>)</td><td><b><code>TTDevice::set_power_state()</code></b> <br> <b><code>TTDevice::wait_eth_core_training()</code></b> <br> <b><code>TTDevice::wait_dram_channel_training()</code></b></td><td>Hardware power-on and training. Puts tensix/MRISC PHYs out of reset, waits for eth and dram cores to be ready, initializes session mutexes.</td></tr>
<tr><td>destructor()</td><td><b>Parameters:</b> <br> - none <br> <b>Returns:</b> <br> - N/A</td><td>1. Set power state to low (<code>TTDevice::set_power_state(false)</code>) <br> 2. Manual RAII cleanup</td><td><b><code>TTDevice::set_power_state()</code></b></td><td>Hardware power-down. Puts tensix/MRISC PHYs into reset.</td></tr>
<tr><td>start_device()</td><td><b>Parameters:</b> <br> - none <br> <b>Returns:</b> <br> - <code>void</code></td><td>1. Acquire mutex (<code>acquire_mutex(CHIP_IN_USE)</code>) <br> 2. Create system memory allocator (<code>SystemMemoryAllocator::create()</code>) <br> 3. Set membars (<code>initialize_membars()</code>) through base writes <br> Lower part optional (defaults to true): <br> 4. Put riscs into reset (<code>TTDevice::assert_risc_reset()</code>) <br> 5. Put riscs out of reset (<code>TTDevice::deassert_risc_reset()</code>) <br> 6. Enable eth queues (<code>TTDevice::send_device_command()</code>) <br> 7. Set clock to busy (<code>TTDevice::set_clock_state()</code>)</td><td><b><code>SystemMemoryAllocator::create()</code></b> <br> <b><code>TTDevice::assert_risc_reset()</code></b> <br> <b><code>TTDevice::deassert_risc_reset()</code></b> <br> <b><code>TTDevice::send_device_command()</code></b> <br> <b><code>TTDevice::set_clock_state()</code></b></td><td>Session bring-up: acquires device lock, configures system memory, initializes barriers, resets and starts riscs, sets clock to full speed. <br><br> <b>Note:</b> System memory setup is owned by <code>SystemMemoryAllocator::create()</code>, which internally handles hardware detection (hugepage/IOMMU), iATU programming, and buffer pinning/mapping. No-op for backends without system memory (JTAG/remote/mock).</td></tr>
<tr><td>stop_device() <br> <i>currently: <code>close_device()</code></i></td><td><b>Parameters:</b> <br> - none <br> <b>Returns:</b> <br> - <code>void</code></td><td>1. Set clock to idle (<code>TTDevice::set_clock_state()</code>) <br> 2. Put riscs into reset (<code>TTDevice::assert_risc_reset(ALL)</code>) <br> 4. Disable ETH queues <br> 5. Release cached IoWindows <br> 6. Release mutex (<code>release_mutex()</code>)</td><td><b><code>TTDevice::set_clock_state()</code></b> <br> <b><code>TTDevice::assert_risc_reset()</code></b></td><td>Session teardown: sets clock to idle, resets riscs, unmaps host memory, releases IoWindows and session lock. Mirror of start_device().</td></tr>
<tr><td>create()</td><td><b>Parameters:</b> <br> - physical_device_id: <code>int</code> — device ordinal <br> - sdesc_path: <code>std::string</code> — SOC descriptor YAML path (default: <code>""</code>, auto-detect) <br> - device_type: <code>IODeviceType</code> — transport type (default: <code>PCIe</code>) <br> <b>Returns:</b> <br> - <code>std::unique_ptr&lt;LocalChip&gt;</code></td><td>1. Create TTDevice (<code>TTDevice::create(ordinal, type)</code>) <br> 2. Initialize device (<code>TTDevice::init_device()</code>) <br> 3. Device Descriptor — byproduct of init <br> 4. Construct Chip from initialized TTDevice (<code>LocalChip(std::move(tt_device))</code>)</td><td><b><code>TTDevice::create()</code></b> <br> <b><code>TTDevice::init_device()</code></b></td><td>Factory: creates and initializes TTDevice, wraps it in a Chip. SocDescriptor comes from TTDevice after init. Sysmem solved by <code>SystemMemoryAllocator</code> / <code>SystemMemoryBuffer</code>.</td></tr>
<tr><td>is_mmio_capable()</td><td><b>Parameters:</b> <br> - none <br> <b>Returns:</b> <br> - <code>bool</code></td><td>1. Return cached capability (<code>Chip::is_mmio_capable()</code>)</td><td>—</td><td>Returns whether chip is locally connected (PCIe). Determined by Chip subclass type (LocalChip=true, RemoteChip=false).</td></tr>
<tr><td>set_clock_state()</td><td><b>Parameters:</b> <br> - state: <code>PowerState</code> — target clock state (BUSY/IDLE) <br> <b>Returns:</b> <br> - <code>void</code></td><td>1. Call clock state change (<code>TTDevice::set_clock_state()</code>)</td><td><b><code>TTDevice::set_clock_state()</code></b> <br> (uses <b><code>TTDevice::send_device_command()</code></b> internally)</td><td>Sets AICLK frequency (BUSY = max, IDLE = min). Note: <code>TTDevice::set_power_state()</code> is a separate operation for hardware power domains — different operation, different name.</td></tr>
<tr><td>wait_for_aiclk_value()</td><td><b>Parameters:</b> <br> - state: <code>PowerState</code> — expected clock state to wait for <br> <b>Returns:</b> <br> - <code>void</code></td><td>1. Poll clock until target reached (<code>wait_for_aiclk_value()</code>) <br> &nbsp;&nbsp;• reads clock (<code>TTDevice::get_clock_freq()</code>) <br> &nbsp;&nbsp;• reads min/max (<code>TTDevice::get_min_clock_freq()</code>, <code>TTDevice::get_max_clock_freq()</code>) <br> &nbsp;&nbsp;• logs temperature (<code>TTDevice::get_asic_temperature()</code>)</td><td><b><code>TTDevice::get_clock_freq()</code></b> <br> <b><code>TTDevice::get_min_clock_freq()</code></b> <br> <b><code>TTDevice::get_max_clock_freq()</code></b> <br> <b><code>TTDevice::get_asic_temperature()</code></b></td><td>Polls AICLK until target value reached. Policy (polling, timeout, logging) is workload; raw reads are base.</td></tr>
<tr><td>get_soc_descriptor()</td><td><b>Parameters:</b> <br> - none <br> <b>Returns:</b> <br> - <code>SocDescriptor&amp;</code></td><td>1. Return descriptor (<code>TTDevice::get_soc_descriptor()</code>)</td><td><b><code>TTDevice::get_soc_descriptor()</code></b></td><td>Passthrough. SocDescriptor is optional on TTDevice, populated after <code>init_device()</code>. Chip reads it from TTDevice — does not maintain its own copy (target design).</td></tr>
<tr><td>write_to_sysmem()</td><td><b>Parameters:</b> <br> - src: <code>const void*</code> — source buffer <br> - offset: <code>uint64_t</code> — offset into system memory buffer <br> - size: <code>uint32_t</code> — bytes to write <br> <b>Returns:</b> <br> - <code>void</code></td><td>1. memcpy into buffer (<code>SystemMemoryBuffer::write_to_sysmem()</code>)</td><td>—</td><td>memcpy into system memory buffer. Pure host-side operation, no device interaction.</td></tr>
<tr><td>read_from_sysmem()</td><td><b>Parameters:</b> <br> - dest: <code>void*</code> — destination buffer <br> - offset: <code>uint64_t</code> — offset into system memory buffer <br> - size: <code>uint32_t</code> — bytes to read <br> <b>Returns:</b> <br> - <code>void</code></td><td>1. memcpy from buffer (<code>SystemMemoryBuffer::read_from_sysmem()</code>)</td><td>—</td><td>memcpy from system memory buffer. Pure host-side operation, no device interaction.</td></tr>
<tr><td>write_to_data()</td><td><b>Parameters:</b> <br> - core: <code>CoreCoord</code> — target core <br> - src: <code>const void*</code> — source buffer <br> - l1_dest: <code>uint64_t</code> — destination address on device <br> - size: <code>size_t</code> — bytes to write <br> <b>Returns:</b> <br> - <code>void</code></td><td>1. Write via data path (<code>TTDevice::write_data()</code>)</td><td><b><code>TTDevice::write_data()</code></b></td><td>Write to device memory via WC-mapped IoWindow. Data path — optimized for throughput.</td></tr>
<tr><td>read_from_data()</td><td><b>Parameters:</b> <br> - core: <code>CoreCoord</code> — target core <br> - dest: <code>void*</code> — destination buffer <br> - l1_src: <code>uint64_t</code> — source address on device <br> - size: <code>size_t</code> — bytes to read <br> <b>Returns:</b> <br> - <code>void</code></td><td>1. Read via data path (<code>TTDevice::read_data()</code>)</td><td><b><code>TTDevice::read_data()</code></b></td><td>Read from device memory via WC-mapped IoWindow. Data path — optimized for throughput.</td></tr>
<tr><td>write_to_regs()</td><td><b>Parameters:</b> <br> - core: <code>CoreCoord</code> — target core <br> - src: <code>const void*</code> — source buffer <br> - reg_dest: <code>uint64_t</code> — register address <br> - size: <code>uint32_t</code> — bytes to write <br> <b>Returns:</b> <br> - <code>void</code></td><td>1. Write via control path (<code>TTDevice::write_ctrl()</code>)</td><td><b><code>TTDevice::write_ctrl()</code></b></td><td>Write to device registers via UC-mapped IoWindow. Control path — ordered, uncached.</td></tr>
<tr><td>read_from_regs()</td><td><b>Parameters:</b> <br> - core: <code>CoreCoord</code> — target core <br> - dest: <code>void*</code> — destination buffer <br> - reg_src: <code>uint64_t</code> — register address <br> - size: <code>uint32_t</code> — bytes to read <br> <b>Returns:</b> <br> - <code>void</code></td><td>1. Read via control path (<code>TTDevice::read_ctrl()</code>)</td><td><b><code>TTDevice::read_ctrl()</code></b></td><td>Read from device registers via UC-mapped IoWindow. Control path — ordered, uncached.</td></tr>
<tr><td>write_to_core_range()</td><td><b>Parameters:</b> <br> - dst: <code>void*</code> — source buffer <br> - size: <code>size_t</code> — bytes to write <br> - core_start: <code>CoreCoord</code> — start of core range <br> - core_end: <code>CoreCoord</code> — end of core range <br> - addr: <code>uint64_t</code> — destination address on each core <br> <b>Returns:</b> <br> - <code>void</code></td><td>1. Multicast write (<code>TTDevice::write_to_core_range()</code>)</td><td><b><code>TTDevice::write_to_core_range()</code></b></td><td>Write same data to a rectangular range of cores via NOC multicast.</td></tr>
<tr><td>dma_write_to_device()</td><td><b>Parameters:</b> <br> - src: <code>const void*</code> — source buffer <br> - size: <code>size_t</code> — bytes to write <br> - core: <code>CoreCoord</code> — target core <br> - addr: <code>uint64_t</code> — destination address <br> <b>Returns:</b> <br> - <code>void</code></td><td>1. DMA H2D (<code>TTDevice::dma_write()</code>)</td><td><b><code>TTDevice::dma_write()</code></b></td><td>DMA host-to-device transfer.</td></tr>
<tr><td>dma_read_from_device()</td><td><b>Parameters:</b> <br> - dst: <code>void*</code> — destination buffer <br> - size: <code>size_t</code> — bytes to read <br> - core: <code>CoreCoord</code> — source core <br> - addr: <code>uint64_t</code> — source address <br> <b>Returns:</b> <br> - <code>void</code></td><td>1. DMA D2H (<code>TTDevice::dma_read()</code>)</td><td><b><code>TTDevice::dma_read()</code></b></td><td>DMA device-to-host transfer.</td></tr>
<tr><td>dma_multicast_write()</td><td><b>Parameters:</b> <br> - src: <code>void*</code> — source buffer <br> - size: <code>size_t</code> — bytes to write <br> - core_start: <code>CoreCoord</code> — start of core range <br> - core_end: <code>CoreCoord</code> — end of core range <br> - addr: <code>uint64_t</code> — destination address on each core <br> <b>Returns:</b> <br> - <code>void</code></td><td>1. DMA multicast (<code>TTDevice::dma_multicast_write()</code>)</td><td><b><code>TTDevice::dma_multicast_write()</code></b></td><td>DMA host-to-device multicast to core range.</td></tr>
<tr><td>l1_membar()</td><td><b>Parameters:</b> <br> - cores: <code>const std::unordered_set&lt;CoreCoord&gt;&amp;</code> — cores to barrier (default: <code>{}</code> = all) <br> <b>Returns:</b> <br> - <code>void</code></td><td>1. Write barrier flags to L1 on specified cores (<code>l1_membar()</code>) <br> 2. Poll until acknowledged <br> (uses base writes as transport)</td><td>—</td><td>Writes barrier values to L1 on specified cores, polls until acknowledged. All logic (which cores, what values, polling) is workload; base layer provides the write mechanism.</td></tr>
<tr><td>dram_membar()</td><td><b>Parameters (overload 1):</b> <br> - cores: <code>const std::unordered_set&lt;CoreCoord&gt;&amp;</code> — DRAM cores to barrier (default: <code>{}</code> = all) <br> <b>Parameters (overload 2):</b> <br> - channels: <code>const std::unordered_set&lt;uint32_t&gt;&amp;</code> — DRAM channels to barrier <br> <b>Returns:</b> <br> - <code>void</code></td><td>1. Write barrier flags to DRAM cores (<code>dram_membar()</code>) <br> 2. Poll until acknowledged <br> Channel overload: converts channels to core coordinates first <br> (uses base writes as transport)</td><td>—</td><td>Same as l1_membar but for DRAM cores. Channel-based overload converts channels to coordinates.</td></tr>
<tr><td>get_risc_reset_state()</td><td><b>Parameters:</b> <br> - core: <code>CoreCoord</code> — target core <br> <b>Returns:</b> <br> - <code>RiscType</code> — set of riscs currently in reset</td><td>1. Read raw reset state (<code>TTDevice::get_risc_reset_state()</code>) <br> 2. Format with type info (<code>ArchitectureImplementation::get_soft_reset_risc_type()</code>) <br> 3. Combine and return (<code>get_risc_reset_state()</code>)</td><td><b><code>TTDevice::get_risc_reset_state()</code></b> <br> <b><code>ArchitectureImplementation::get_soft_reset_risc_type()</code></b></td><td>Reads raw reset register and formats it with risc type information. Raw reads are base; combining is workload.</td></tr>
<tr><td>assert_risc_reset() (per-core)</td><td><b>Parameters:</b> <br> - core: <code>CoreCoord</code> — target core <br> - selected_riscs: <code>const RiscType</code> — which riscs to reset <br> <b>Returns:</b> <br> - <code>void</code></td><td>1. Assert reset on core (<code>TTDevice::assert_risc_reset()</code>)</td><td><b><code>TTDevice::assert_risc_reset()</code></b></td><td>Raises soft reset signal for specified riscs on one core. Passthrough.</td></tr>
<tr><td>deassert_risc_reset() (per-core)</td><td><b>Parameters:</b> <br> - core: <code>CoreCoord</code> — target core <br> - selected_riscs: <code>const RiscType</code> — which riscs to release <br> - staggered_start: <code>bool</code> — stagger risc start times <br> <b>Returns:</b> <br> - <code>void</code></td><td>1. Deassert reset on core (<code>TTDevice::deassert_risc_reset()</code>)</td><td><b><code>TTDevice::deassert_risc_reset()</code></b></td><td>Lowers soft reset signal for specified riscs on one core. Passthrough.</td></tr>
<tr><td>assert_risc_reset() (all cores)</td><td><b>Parameters:</b> <br> - selected_riscs: <code>const RiscType</code> — which riscs to reset <br> <b>Returns:</b> <br> - <code>void</code></td><td>1. For each TENSIX core (<code>assert_risc_reset()</code> loop): <br> &nbsp;&nbsp;• Assert reset (<code>TTDevice::assert_risc_reset()</code>)</td><td><b><code>TTDevice::assert_risc_reset()</code></b></td><td>Iterates over all TENSIX cores calling per-core base API. Loop is workload; per-core reset is the base primitive.</td></tr>
<tr><td>deassert_risc_reset() (all cores)</td><td><b>Parameters:</b> <br> - selected_riscs: <code>const RiscType</code> — which riscs to release <br> - staggered_start: <code>bool</code> — stagger risc start times <br> <b>Returns:</b> <br> - <code>void</code></td><td>1. For each TENSIX core (<code>deassert_risc_reset()</code> loop): <br> &nbsp;&nbsp;• Deassert reset (<code>TTDevice::deassert_risc_reset()</code>)</td><td><b><code>TTDevice::deassert_risc_reset()</code></b></td><td>Iterates over all TENSIX cores calling per-core base API. Loop is workload; per-core reset is the base primitive.</td></tr>
<tr><td>get_clock()</td><td><b>Parameters:</b> <br> - none <br> <b>Returns:</b> <br> - <code>int</code> — current AICLK frequency</td><td>1. Get clock (<code>TTDevice::get_clock_freq()</code>)</td><td><b><code>TTDevice::get_clock_freq()</code></b></td><td>Returns current AICLK frequency. Passthrough.</td></tr>
<tr><td>get_numa_node()</td><td><b>Parameters:</b> <br> - none <br> <b>Returns:</b> <br> - <code>int</code> — NUMA node ID</td><td>1. Get NUMA node (<code>TTDevice::get_numa_node()</code>)</td><td><b><code>TTDevice::get_numa_node()</code></b></td><td>Returns NUMA node for this device. -1 if not PCIe-connected or non-NUMA. Passthrough to <code>PcieInterface::get_numa_node()</code>.</td></tr>
<tr><td>send_device_command()</td><td><b>Parameters:</b> <br> - msg_code: <code>uint32_t</code> — command code <br> - args: <code>const std::vector&lt;uint32_t&gt;&amp;</code> — command arguments (default: <code>{}</code>) <br> - timeout_ms: <code>std::chrono::milliseconds</code> — timeout (default: ARC_MESSAGE_TIMEOUT) <br> <b>Returns:</b> <br> - <code>DeviceCommandResult{exit_code, return_values}</code></td><td>1. Send command to device (<code>TTDevice::send_device_command()</code>)</td><td><b><code>TTDevice::send_device_command()</code></b></td><td>Generic device command interface. Hides ArcMessenger behind TTDevice API. Returns <code>DeviceCommandResult{exit_code, return_values}</code>.</td></tr>
<tr><td>set_remote_transfer_ethernet_cores()</td><td><b>Parameters (overload 1):</b> <br> - cores: <code>const std::unordered_set&lt;CoreCoord&gt;&amp;</code> — ethernet cores <br> <b>Parameters (overload 2):</b> <br> - channels: <code>const std::set&lt;uint32_t&gt;&amp;</code> — ethernet channels <br> <b>Returns:</b> <br> - <code>void</code></td><td>1. Translate CoreCoord/channel to xy_pair (<code>set_remote_transfer_ethernet_cores()</code>) using SocDescriptor <br> 2. Configure remote comm (<code>RemoteCommunication::set_remote_transfer_ethernet_cores()</code>)</td><td><b><code>RemoteCommunication::set_remote_transfer_ethernet_cores()</code></b></td><td>Configures which ethernet cores are used for remote transfer. Both LocalChip (broadcast) and RemoteChip (point-to-point) translate using local chip's SocDescriptor.</td></tr>
<tr><td>enable_ethernet_queue()</td><td><b>Parameters:</b> <br> - timeout_ms: <code>std::chrono::milliseconds</code> — timeout (default: ETH_QUEUE_ENABLE_TIMEOUT) <br> <b>Returns:</b> <br> - <code>void</code></td><td>1. Enable eth queue (<code>TTDevice::send_device_command()</code>)</td><td><b><code>TTDevice::send_device_command()</code></b></td><td>Sends ARC command to enable ethernet queue. Hidden behind <code>send_device_command()</code>.</td></tr>
<tr><td>set_barrier_address_params()</td><td><b>Parameters:</b> <br> - barrier_address_params: <code>const BarrierAddressParams&amp;</code> — L1 and DRAM barrier addresses <br> <b>Returns:</b> <br> - <code>void</code></td><td>1. Set barrier params (<code>set_barrier_address_params()</code>)</td><td>—</td><td>Configuration for membar addresses. Drives l1_membar/dram_membar behavior.</td></tr>
</table>
